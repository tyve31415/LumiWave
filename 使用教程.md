# 用 LumiWave（示波器合成器）创作芯片音乐 · 零基础教程

> 芯片音乐（Chiptune）：那种 8-bit 游戏机、红白机里的电子音乐——由简单的方波、三角波、噪声组成，清脆、复古、有「电子味」。

本教程假设你完全没接触过音乐制作。不需要任何音乐基础：打开应用，写一行简单的公式，就能立刻听到声音，并把多个声音叠起来变成一首曲子。

---

## 1. 打开应用，先听一首

**桌面版**：安装好依赖后，在项目文件夹运行 `npm start`（或用打包出的 LumiWave 应用）。

应用是一个**多窗口固定分屏桌面**（无页面滚动）：📟 主示波器、🗂 收藏夹·音乐、🎹 混音器、🕘 时间线、🔢 音序器各是一个窗口，互不重叠；顶栏「🪟 窗口」下拉菜单可随时重开/聚焦任意窗口。「🗂 收藏夹」与「🎧 本地音乐」已合并为一个窗口，固定在桌面最左列（位置与大小恒定，禁止拖动/缩放/停靠，无锁按钮）；步进音序器位于最右列。「🎛 通道控制」是**弹出层**：从「🪟 窗口」菜单打开，悬浮覆盖在其他窗口之上（默认隐藏），**可自由拖动**（位置自动记忆，默认解锁，可用 🔒 锁定），点击窗口外任意处或菜单内再点一次收起。其余窗口右上角有 **🔒/🔓** 按钮：解锁后可在空白处拖动、缩放，或拖到其他窗口上**分屏停靠**（VS Code 式）。音乐播放器的小波形与频谱默认折叠为**二级菜单**，点击「波形 / 频谱」条展开显示。

打开「🎹 混音器」窗口，上方有一个「🎵 Funky Stars」区域，点里面的 **▶ 播放**，你会听到一首完整的芯片音乐（内置演示曲，由真实 MIDI 自动转录而来）。再点一下 **■ 停止** 就停。

📟 主示波器窗口显示总波形/频谱，以及各通道（CH1–CH4）的彩色独立波形；「🎹 混音器」窗口里还有一块 16 声部分栏示波器（左右两栏各 8 条）。

先玩一下，感受感受芯片音乐长什么样。

**常用快捷键**：

| 快捷键 | 功能 |
| --- | --- |
| 空格 | 播放 / 停止合成器 |
| Esc | 全部停止 |
| Ctrl+O | 导入 MIDI |
| Ctrl+Shift+O | 导入音频 |
| Ctrl+E | 导出 WAV |
| Ctrl+Enter | 播放 / 停止 |
| Ctrl+D | 播放演示曲 |
| Ctrl+R | 开始 / 停止录制 |
| Ctrl+T | 波形 / 频谱切换 |
| F11 | 全屏 |

---

## 2. 声音的本质：波形

扬声器发声，靠的是里面一张膜片**来回振动**，带动空气振动，传到你耳朵里。

在这个应用里，你写一个函数 `f(t)`，它回答一个问题：**在时间 `t` 这一刻，膜片应该振到哪个位置？**

- 函数返回值在 `-1` 到 `+1` 之间：`1` 是振到最前，`-1` 是振到最后，`0` 是正中间。
- 膜片快速来回振 → 音高；振得慢 → 音低。
- 「📟 主示波器」窗口里那条绿色波形线，画的就是这个振动随时间变化的形状——也就是「波形」。

---

## 3. 三个关键数字

写函数时你会反复遇到这几个量：

- `t`：**时间**（单位秒）。它是自变量，从 0 开始不停增长。
- `freq`：**当前频率**（单位赫兹 Hz），即每秒振动多少次。数字越大音越高。标准音 A4 = 440 Hz。
- `TWO_PI`：数学常数 2π ≈ 6.28。画波形时照抄即可，先不用深究。

---

## 4. 你的第一个声音

在「混音器」面板找到第一行（V1），把它的函数框改成：

```js
sin(TWO_PI * freq * t) * 0.5 * env
```

然后点「⚡ 应用全部函数」，再点「▶ 播放」。你应该听到一个持续、圆润的「嗡——」声，这就是一个 440 Hz 的正弦波。

拆解这行公式：

- `sin(...)`：正弦函数，画出一条光滑波形，声音圆润。
- `TWO_PI * freq * t`：让波形按 `freq` 的频率振动。
- `* 0.5`：把音量减半（返回值落在 -0.5 到 +0.5）。
- `* env`：乘上「包络」，让声音只在有音符时响（见第 6 节）。

**试一下**：拖动「播放」面板里的「频率」滑块，音高会跟着变——这就是在改 `freq`。

---

## 5. 波形家族（音色）

把函数里的 `sin` 换成别的，就能得到不同「音色」（音色 = 波形形状）：

| 函数 | 听感 | 示例 |
|------|------|------|
| `sin` | 圆润、柔和 | `sin(TWO_PI * freq * t) * 0.5 * env` |
| `sq` | 清脆、电子味十足（游戏机经典） | `sq(TWO_PI * freq * t) * 0.25 * env` |
| `saw` | 明亮、有「锯齿感」 | `saw(TWO_PI * freq * t) * 0.25 * env` |
| `tri` | 柔和，介于正弦与方波之间 | `tri(TWO_PI * freq * t) * 0.4 * env` |
| `pulse` | 窄方波，更「尖」 | `pulse(TWO_PI * freq * t, 0.4) * 0.2 * env` |
| `noise` | 沙沙声，像鼓/镲 | `noise() * 0.3` |

> 注意：`sq` / `saw` / `tri` / `pulse` 括号里也要写 `TWO_PI * freq * t`；`noise` 不用参数，直接 `noise()`。

**小练习**：把 V1 改成 `sq(...)`，对比和 `sin(...)` 的区别。这就是芯片音乐里最常用的「方波」。

---

## 6. 包络：让音符有「呼吸」

如果去掉公式里的 `* env`，声音会一直响个不停。加上 `* env`，声音就只在「按下一个音符」时响，松手后慢慢消失。

「包络」面板有两个滑块：

- **起音 attack**：音符按下后，声音从 0 涨到最大所需的时间。调大一点，声音像「渐入」。
- **释音 release**：音符松开后，声音从最大掉到 0 所需的时间。调大一点，声音像「余音绕梁」。

`env` 是一个 0~1 的数字，会根据起音/释音设置自动变化。你只需在公式里乘上它。

---

## 7. 混音器：把多个声部叠起来

一首音乐通常是好几个声音同时响（旋律 + 贝斯 + 鼓……）。应用里有 **16 个声部**（V1~V16），每个声部都可以写一个自己的函数，它们会**叠加**在一起播放。

- 每行左边的 `V1` 按钮：开/关这个声部（亮绿色 = 开）。
- 「增益」滑块：单独调这个声部的音量。
- 中间的下拉框：一键套用预设波形。
- 函数框：写你自己的公式。

**叠加示例**：让 V1 和 V2 同时打开，V1 用正弦、V2 用方波，就能听到两个声音混在一起——这就是「混音」。

> 小技巧：不同声部用不同波形、不同音量，堆出层次。别让总音量太大——如果波形「顶到屏幕边缘」，说明削波失真了，把音量调小。

---

## 8. 写出你的旋律（音符与 seq）

`freq` 是「当前频率」，但如果你想让它**自动变音**，可以用 `seq`（序列）函数：

```js
sin(TWO_PI * seq(['A4', 'C5', 'E5', 'A5'], t, 0.25) * t) * 0.4 * env
```

`seq([...], t, 0.25)` 的意思是：每 0.25 秒依次取出列表里的一个音符，循环播放。上面会循环 A4 → C5 → E5 → A5，形成一段「琶音」。

音符用「音名 + 数字」表示：

- `A4` 是标准音（440 Hz），数字是「八度」，数字越大音越高。
- `C5`、`E5`、`A5` 是比 A4 更高的音。
- 也可以直接写频率数字：`seq([440, 523.25, 659.25], t, 0.25)`。

`seq` 的第三个参数是「每个音的时长（秒）」，改小一点就是更快的琶音。

> 想单独查某个音名的频率？用 `n('A4')`，它会返回 A4 的频率数值。

---

## 9. 步进音序器：像打节拍一样写曲子

不想写 `seq` 的话，可以用「🔢 音序器」窗口（独立 CH4 大通道）：

1. 给某个声部（比如 V1）写个简单波形，例如 `sq(TWO_PI * freq * t) * 0.25 * env`。
2. 到「步进音序器」面板点「▶ 播放音序」。
3. 下面有 16 个圆点（LED），亮着 = 这一拍响，暗着 = 静音。**点圆点可开关每一拍**。
4. 每个圆点上方的下拉框，可给这一拍选音高（音名）。
5. 「速度」滑块调快慢（BPM，每分钟拍数）。
6. 「随机」按钮一键随机生成节奏。

音序器会自动改变 `freq`，所以你的声部会跟着音序器的音符走——这是写「循环旋律」最直观的方式。

---

## 10. 键盘演奏

- 「🎹 混音器」窗口底部有一个两八度钢琴，鼠标点它就能弹。
- 也能用**电脑键盘**弹：低八度 `Z S X D C V G B H N J M`，高八度 `Q 2 W 3 E R 5 T 6 Y 7 U`。
- 按**空格键** = 播放/停止。

同时按住多个键可以弹出「和弦」。

---

## 11. 时间线编辑器与大通道

**时间线（🕘 窗口，独立 CH2 通道）**：钢琴卷帘式编辑，每条轨道是一个声部（V1–V16），先给轨道选波形，再在网格里点出音符：

1. 点击轨道空白处添加音符（音高用「音高」下拉框设定）。
2. 拖动音符移动位置；拖动音符右边缘调整时长。
3. 点选音符后用 Delete 键或「删除选中」移除。
4. 点「▶ 播放」按时间线演奏，与混音器互不干扰。

**大通道（🎛 顶栏弹出层）**：混音器（CH1）、时间线（CH2）、本地音乐（CH3）、音序器（CH4）各是一条独立通道。每条通道可开关、调音量，并可切换 **⛓ 合并**（汇入主混音）或 **⚡ 独立**（SOLO 单独监听）——有通道独立时只输出独立通道。在「🗂 收藏夹」窗口点击音频文件，就会自动载入 CH3「🎧 本地音乐」播放，主示波器同步显示其波形。

---

## 12. 导入 MIDI：让应用自动扒谱

如果你有现成的 `.mid` 文件（网上很多地方能下载），直接**拖到窗口上**，或点「📁 导入 MIDI」（弹出系统文件对话框，菜单里还有「最近文件」记录）。

应用会自动：

1. 解析 MIDI 里的音符、速度、拍号。
2. 分析出各通道（旋律 / 贝斯 / 鼓组……）。
3. 分配给 16 个声部，并自动开始播放。

> 最多支持 16 通道，超过会截断。自动分析的音色分配是「猜」的，不满意可自己在混音器里改波形/增益。想听回内置演示曲，点「🎵 演示」。

---

## 13. 录制导出成 WAV

- 点「● 录制」开始录音（按钮变成「■ 停止录制」）。
- 再点一下停止，会弹出系统「另存为」对话框，选好位置即可保存为 WAV。

录下来的是混音后的最终声音。

---

## 14. 常用函数速查表

| 类别 | 函数 | 说明 |
|------|------|------|
| 变量 | `t` | 时间（秒） |
| 变量 | `freq` | 当前频率（Hz） |
| 变量 | `env` | 包络 0~1 |
| 变量 | `SR` | 采样率 |
| 波形 | `sin(x) cos(x) tan(x)` | 三角函数 |
| 波形 | `sq(x) saw(x) tri(x)` | 方波 / 锯齿波 / 三角波 |
| 波形 | `pulse(x, w)` | 脉冲波（w 控制宽窄） |
| 波形 | `noise()` | 白噪声 |
| 音符 | `n('A4')` | 音名转频率 |
| 音符 | `seq([...], t, dur)` | 循环播放音符序列 |
| 音符 | `midiToFreq(m)` | MIDI 编号转频率 |
| 数学 | `PI` `TWO_PI` | 圆周率、2π |
| 数学 | `pow sqrt exp log abs` | 幂 / 开方 / 指数 / 对数 / 绝对值 |
| 数学 | `floor ceil round min max` | 取整 / 比较 |
| 数学 | `random()` | 随机数 0~1 |
| 工具 | `clamp(v, a, b)` | 把 v 限制在 a~b 之间 |
| 工具 | `lerp(a, b, x)` | 线性插值 |

---

## 15. 从零做一首小曲子（完整示例）

按下面步骤，做一段最简单的芯片小曲：

**第一步**：V1 设主旋律（方波），用 `seq` 循环六个音：

```js
sq(TWO_PI * seq(['A4', 'C5', 'E5', 'A5', 'E5', 'C5'], t, 0.2) * t) * 0.22 * env
```

**第二步**：V2 设贝斯（锯齿波），低八度、慢一倍：

```js
saw(TWO_PI * seq(['A2', 'A2', 'E2', 'A2'], t, 0.5) * t) * 0.2 * env
```

**第三步**：V3 设鼓点（噪声，每 0.5 秒「敲」一下）：

```js
noise() * exp(-9 * (t % 0.5)) * 0.4
```

**第四步**：点「⚡ 应用全部函数」，再点「▶ 播放」。

你就有了一段有旋律、有贝斯、有鼓的循环小曲子！

想更丰富？试试：

- 把「滑音」滑块调大，音高会平滑滑动。
- 在「🕘 时间线」窗口排一段旋律，与音序器同时播放（两条独立通道）。
- 换不同波形，或叠第 4、第 5 个声部。

---

## 16. 常见问题与避坑

- **改了函数没反应？** 记得点「⚡ 应用全部函数」。
- **没声音？** 确认声部左边按钮是亮的（开）、音量不是 0、且点了「▶ 播放」。
- **声音很吵/失真？** 总音量太大，把「音量」或各声部「增益」调小。
- **波形顶到屏幕边？** 说明信号超过 ±1 被削波了，降音量。
- **函数报错？** 检查括号是否配对、函数名是否拼错、是否漏了 `* t` 或 `TWO_PI`。
- **在函数框里打字没反应？** 已修复，正常输入即可；钢琴键只在焦点不在输入框时生效。
- **导入的 MIDI 音色怪？** 自动分析是猜的，手动改对应声部的波形/增益即可。

---

祝你玩得开心，做出自己的第一首芯片音乐！🎵